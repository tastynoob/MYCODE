#ifndef KEYS_H
#define KEYS_H

//指定循环次数,fre:循环次数,par:指定(循环次数-1)的参数名
#define cyc(fre,par)  \
for (int par = 0; par < (fre); par++) 

#define defs(name) \
struct name 





















#endif
